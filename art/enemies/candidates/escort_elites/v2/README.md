# 精英与随从立绘候选 v2

按用户“去噪点，加强手绘感”要求，使用内置 `image_gen` 逐张编辑 v1 的十张原图。

修改：去除颗粒、细碎磨损与杂乱材质斑点，简化碎面和褶皱，强化有粗细变化的墨线、大色块与明确阴影。保留角色身份、动作、装备和主要配色。

`overview.png` 为四组编队总览；四张 `act*.png` 为分组预览；十张 `escort_*.png` 为原始生成候选。完整提示词及来源见 `candidates.json`。所有原图按生成结果原样复制，预览仅做临时品红底分离与排版，不代表最终场景缩放。

状态：用户2026-09-17确认实装。十张生产透明PNG已导出到 `art/enemies/SPR_Enemy_Escort*.png` 并更新敌人数据和生产映射。主怪与随从主体外接矩形面积约3:2，实际比例预览为 `production-preview.png`；旧的 `overview.png` 仅展示候选造型，不代表游戏大小。

Godot 4.7.1验证：资源回归569/569、编队图形与功能回归159/159。四套编队与补兵实际截图已检查，显示面积比1.4985～1.5008。移动设备实测：NOT VERIFIED。

2026-09-18运行时尺寸修正：游戏改为裁去透明留白后按完整编队统一适配可用区域，解决整组偏小。生产PNG不改；本目录预览用于资源审阅，绝对显示尺寸以实际CombatPlay为准。

依据 `AI_STUDIO/Design/Art/ART_STYLE_ENEMIES.md` 第 7 节：“新增或重做怪物时，先提交独立候选供用户审批；审批前不覆盖生产资源。”
