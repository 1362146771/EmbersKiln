# 精英编队立绘候选 v1

状态：造型待用户审批，尚未接入生产资源。

使用内置 `image_gen` 生成十种独立立绘，四组编队共十二个位置；同类突击兵、倒计时随从复用同一候选。完整提示词、参考图和原始生成路径见 `candidates.json`。

## 审阅入口

- `overview.png`：四组总览，编号 01–10。
- `act2_command.png`：指挥官、两名突击兵。
- `act2_guard.png`：主攻手、护卫、干扰兵。
- `act3_bombs.png`：投放者、两名倒计时随从。
- `act3_observers.png`：监视主攻手、技能监视者、能力监视者。
- 十张 `escort_*.png`：1254 × 1254 原始候选。

## 技术状态

十张原图均与生成文件逐字节一致，检查结果见 `source-check.json`。六张带原生透明通道，四张保留品红底。总览仅在内存中临时去除品红底并排版，未改写原图；预览中的相对尺寸不代表最终战斗缩放。

已检查原图与编队预览。生产透明边缘、Godot 导入及场景验证：**NOT VERIFIED**，待造型批准后执行。

依据 `AI_STUDIO/Design/Art/ART_STYLE_ENEMIES.md` 第 7 节：“新增或重做怪物时，先提交独立候选供用户审批；审批前不覆盖生产资源。”本候选包未修改生产映射或敌人数值。
